# Secrets
.env
.env.local
.env*.local
.claude/settings.local.json

# Dependencies / build
node_modules/
.next/
out/
dist/
*.tsbuildinfo

# Test + tooling output
coverage/
playwright-report/
test-results/
.eslintcache

# OS / editor
.DS_Store
*.swp

# Generated by `pnpm grammars` from node_modules (do not commit ~4.7MB of wasm)
public/grammars/

# Agent tooling — local to each developer's machine, not part of the project.
# The hallmark skill is vendored per-machine; skills-lock.json records how to
# re-install it (source: nutlope/hallmark).
.agents/
.claude/

# MCP server config — per-machine, and its 21st.dev key comes from a local env var.
.mcp.json
opencode.json

# Git worktrees — local checkouts, never committed.
.worktrees/
